# Supabase project config for the helpdesk agent's conversation history.
# Linked project ref: viqbdqhdgzfaauhuekar
project_id = "K4A-DAY04-ThreeMan"

[db]
# Match the platform's current major so local and hosted behave the same.
major_version = 15

[api]
enabled = true
schemas = ["public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000
